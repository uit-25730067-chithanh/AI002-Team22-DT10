# Core dependencies for AI002 - Team 10 (Coffee Price Forecasting)
# python>=3.9
#
# KISS: No venv required. Install directly to system Python:
#   pip install -r requirements.txt
# (Team is small, single project per machine, no version conflicts.)

# Data manipulation & ML
pandas>=2.0        # DataFrame ops, CSV I/O, time-series handling
numpy>=1.24        # Numerical arrays, random generators for mock data
scikit-learn>=1.3  # Random Forest model, train/test split, metrics (MAE/RMSE)
joblib>=1.3        # Persist trained model to .pkl file

# Optional (post-holiday model comparison)
# xgboost>=2.0  # Gradient-boosted trees baseline compare. Requires libomp on macOS: brew install libomp

# Backend API
fastapi>=0.104     # REST API framework + auto-generated Swagger UI (/predict, /health)
uvicorn[standard]>=0.24  # ASGI server to run FastAPI
pydantic>=2.0      # Input validation (Robustness pillar) + type-safe request/response models
python-multipart   # File upload support (if Team 1 uploads CSV via API)

# Visualization & EDA
matplotlib>=3.7    # Plots for EDA notebook + feature importance charts
seaborn>=0.12      # Statistical plots (correlation heatmap, distribution)

# Jupyter notebooks
jupyter            # JupyterLab server
notebook           # Classic notebook runtime for EDA

# Data crawling and processing
beautifulsoup4>=4.12.0
aiohttp>=3.9.0
pandas>=2.0.0
python-dateutil>=2.8.2
requests>=2.31.0
